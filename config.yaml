# =============================================================================
# COMPATIBILITÉ RACINE
# =============================================================================
# Ce fichier est conservé pour les scripts, notebooks et commandes historiques.
# La configuration réelle est maintenant structurée sous `config/` :
# - `config/base.yaml`
# - `config/validation/proxies.yaml`
# - `config/scenarios/*.yaml`
extends: config/scenarios/xynthia_winter_night.yaml
