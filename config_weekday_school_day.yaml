# =============================================================================
# COMPATIBILITÉ RACINE
# =============================================================================
# Alias historique vers le scénario désormais maintenu dans `config/scenarios/`.
extends: config/scenarios/weekday_school_day.yaml
