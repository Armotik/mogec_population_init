[pytest]
addopts = -ra --strict-markers
markers =
    unit: tests unitaires rapides sans dependance aux jeux de donnees locaux.
    integration: tests d'integration utilisant le pipeline et/ou les donnees locales.
    slow: tests integration couteux (runtime superieur ou composants lourds).
